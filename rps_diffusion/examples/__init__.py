"""Runnable example scenarios.

Run all of them with ``python -m rps_diffusion.examples`` or a single one
with e.g. ``python -m rps_diffusion.examples.b_asymmetric_rates``. Output (videos
and plots) is written to ``examples/output/`` below the current directory.
"""
